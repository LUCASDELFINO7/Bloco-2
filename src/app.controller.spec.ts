// API NEST

// métodos -> get ,post, put ,delete

// GET - O GET  PUXA/LÊ/ PEGAR/REQUERER uma variável de outra classe do projeto, como se pegasse o dado de outro lugar dentro do projeto.

// POST -> ENVIA OS DADOS/ CRIAR ALGUM DADO

// PUT -> É COMO SE FOSSE UM UPDATE ALTERA/ATUALIZA UM DADO JÁ EXISTENTE.

// DELETE -> : operador que permite a deleção de dados.



// Contra C - para finalizar o projeto para atualizar caso você precise alterar algo

// npm run start:dev  -> ele vai atualizar sem ficar precisando reiniciar o servidor, ele é igual o Contra C , mas não precisa ficar parando o servidor, vai automático.


// Repository -> Cada entidade(TABELA) possui seu próprio repositório que trata de todas as operações com sua entidade. Repository é como,  EntityManager , mas suas operações são limitadas a uma entidade concreta. Você pode acessar o repositório via EntityManager.

// CONTROLLER - > o controller é responsável por controlar as rotas , ele apenas direciona,por  exemplo, https://docs.google.com/document/ , observa que ele traça a rota /document do HTTP, por isso o controller vai controlar quem vai fazer o serviço que é a nossa SERVICE.


// PROVIDERS ->

// SERVICE -> camada de lógica é a lógica.

// MODULE -> ele é responsável por organizar nossos códigos, gerenciar nossos códigos da forma que deve ocorrer. AQUI por exemplo conecta o banco de dados.

// Entities -> é a criação das tabelas
